# ============================================================
# TSG Salary Calculator — Frontend (Vite / React)
# No client-side auth configuration required.
# Authentication is handled server-side.
# ============================================================
